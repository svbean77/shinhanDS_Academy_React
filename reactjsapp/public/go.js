import { arr, arr2, getName, info } from "./common.js"; // 어떤 값을 가져와 사용할지 중괄호 안에 작성
arr2[1] = 88;
console.log("go:", info.loc);
console.log("go:", arr);
console.log("go:", arr2);
console.log("go:", getName());
